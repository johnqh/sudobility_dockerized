# Sudobility Dockerized - Environment Variables Template
#
# This file documents all environment variables used by the containers.
# Actual values are managed via Doppler and stored in config-generated/.env.*
#
# DO NOT commit real values to version control!

# =============================================================================
# shapeshyft_api
# =============================================================================

# Server Configuration (non-sensitive, have defaults)
PORT=3000
NODE_ENV=production

# Database (REQUIRED - from Doppler)
# PostgreSQL connection string
DATABASE_URL=postgres://user:password@host:port/database

# Encryption (REQUIRED - from Doppler)
# 64-character hex string for AES-256 encryption
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Firebase (REQUIRED - from Doppler)
# Firebase Admin SDK credentials
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# RevenueCat (OPTIONAL - from Doppler)
# Used for rate limiting, gracefully degrades if not set
REVENUECAT_API_KEY=

# =============================================================================
# Doppler Configuration
# =============================================================================
#
# Each container has its own Doppler service token stored in:
#   .doppler-token-<container_name>
#
# To set up Doppler:
# 1. Create a Doppler project for this deployment
# 2. Create configs for each container (e.g., shapeshyft_api)
# 3. Add the environment variables above to each config
# 4. Generate service tokens for each config
# 5. Run ./setup.sh and enter the tokens when prompted
#
# =============================================================================
