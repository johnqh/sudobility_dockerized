# Default environment variables for shapeshyft_api
# Non-sensitive values only - secrets come from Doppler

# Server Configuration
PORT=3000
NODE_ENV=production

# =============================================================================
# REQUIRED SECRETS (must be set via Doppler)
# =============================================================================
# DATABASE_URL=postgres://user:password@host:port/database
# ENCRYPTION_KEY=<64-character-hex-string>
# FIREBASE_PROJECT_ID=<your-firebase-project-id>
# FIREBASE_CLIENT_EMAIL=<service-account-email>
# FIREBASE_PRIVATE_KEY=<private-key-with-newlines>

# =============================================================================
# OPTIONAL SECRETS (set via Doppler if needed)
# =============================================================================
# REVENUECAT_API_KEY=<revenuecat-api-key>
